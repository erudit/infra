---
- name: Install everything
  hosts: all
  sudo: no
  tasks:
    - name: Bootstrap distribution with ansible prerequisite
      script: >
        scripts/bootstrap_ansible_apt.sh
        creates=/usr/bin/python
